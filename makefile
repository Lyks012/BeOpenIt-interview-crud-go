
# ENVIRONNEMENT = "dev"
# REPOSITORY = "beopenmairieapi"
# TAG ?= ""
# COMMIT ?= ""


build:
	# docker build -t ${ENVIRONNEMENT}/${REPOSITORY}/${TAG}
	# docker push ${ENVIRONNEMENT}/${REPOSITORY}/${TAG}
	echo building
	echo building finished

test:
	echo testing
	echo testing finished
deploy:
	echo deploying
	echo deploy finished